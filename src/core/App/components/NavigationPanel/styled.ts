import styled from "styled-components";

export const StyledNavigationPanel = styled.nav`
    color: ${({ theme }) => theme.colors.white};
    background-color: ${({ theme }) => theme.colors.black};
    height: 94px;
`;